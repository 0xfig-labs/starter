# Assets

Put assets imported by source code here, for example:

```tsx
import logoUrl from "@/assets/logo.svg";
```

Use `public/assets/` instead for files that must keep a stable URL and should not be processed by the bundler.
