# Public assets

Files here are served as-is from `/assets/...`.

Use this for stable public URLs such as app screenshots, external integration assets, or files referenced outside the module graph.
Prefer `src/assets/` for assets imported by React/TypeScript code.
